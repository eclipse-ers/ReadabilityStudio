```{r}
#| include: false
#| eval: true

source('R/appdown.r')
```

# macOS `r os_logo('apple')`

| Requirements |
| :-- |
| macOS {{< var MACOS_REQUIRED >}} (or higher) |
| Apple Silicon or Intel processor |
| {{< var INSTALLED_SIZE_MB >}} MB of disk space |
| {{< var MIN_RAM >}} of RAM ({{< var RAM_RECOMMENDED >}} recommended) |

## Installing {-}

Open "ReadabilityStudio.dmg" and drag-and-drop the program into the "Applications" folder.
This will copy "Readability Studio.app" (the self-contained program) into where the rest of your applications are installed.

## Uninstalling {-}

From the "Applications" folder, drag "Readability Studio.app" into the Trash bin.

## Updating {-}

Open "ReadabilityStudio.dmg" and drag-and-drop the program into the "Applications" folder, replacing the existing copy.
