VERSION: "${FULL_DISPLAY_VERSION}"
FULL_VERSION: "${FULL_VERSION}"
BUILD_YEAR: "${BUILD_YEAR}"
PROGRAM_NAME: "${readstudio_DESCRIPTION}"
PROGRAM_LONGNAME: "${PROGRAM_LONGNAME}"
PROGRAM_AUTHOR: "${PROGRAM_AUTHOR}"
PROGRAM_DESCRIPTION: "${PROGRAM_COMMENTS}"
PUBLISHER: "${PUBLISHER}"
MACOS_REQUIRED: "${CMAKE_OSX_DEPLOYMENT_TARGET}"
WIN32_REQUIRED: "${WIN32_DEPLOYMENT_TARGET}"
MIN_RAM: "${MIN_RAM}"
RAM_RECOMMENDED: "${RAM_RECOMMENDED}"
UNIX_ARCHITECTURE: "${UNIX_ARCHITECTURE}"
BUILD_TARGET: "${CMAKE_PROJECT_NAME}"
GIT_REPO: "${GIT_REPO}"
ISSUE_PAGE: "${ISSUE_PAGE}"
MIN_GCC_REQUIRED: "${MIN_GCC_REQUIRED}"
MIN_APPLECLANG_REQUIRED: "${MIN_APPLECLANG_REQUIRED}"
MIN_WXWIDGETS_REQUIRED: "${MIN_WXWIDGETS_REQUIRED}"
CMAKE_MINIMUM_REQUIRED_VERSION: "${CMAKE_MINIMUM_REQUIRED_VERSION}"
MIN_MSVC_TOOLSET_REQUIRED: "${MIN_MSVC_TOOLSET_REQUIRED}"
MIN_MSVC_REQUIRED: "${MIN_MSVC_REQUIRED}"
SNOWBALL_STANDARD: "${SNOWBALL_MAJOR_VERSION}.${SNOWBALL_MINOR_VERSION}.${SNOWBALL_PATCH_VERSION}"
CMAKE_CXX_STANDARD: "${CMAKE_CXX_STANDARD}"
INSTALLED_SIZE_MB: "${INSTALLED_SIZE_MB}"
